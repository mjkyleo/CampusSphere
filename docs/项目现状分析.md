# 校园生活平台 — 项目现状分析报告

> 面向对象：Google AI Studio（前端开发辅助）
> 配套文件：`docs/API_Reference.md`（88 个接口完整文档）、`docs/openapi.json`（OpenAPI 3.x，可导入 Postman/Apifox）
> 本报告补充 API 文档之外的全部上下文：架构、数据库、认证、中间件、WebSocket、前端现状与对接要求。

---

## 目录

1. [项目概览](#1-项目概览)
2. [技术栈总览](#2-技术栈总览)
3. [系统架构与请求链路](#3-系统架构与请求链路)
4. [目录结构](#4-目录结构)
5. [当前运行服务与端口](#5-当前运行服务与端口)
6. [数据库设计](#6-数据库设计)
7. [认证与授权](#7-认证与授权)
8. [中间件与基础设施](#8-中间件与基础设施)
9. [API 规范与约定](#9-api-规范与约定)
10. [API 模块清单](#10-api-模块清单)
11. [前端现状与缺口](#11-前端现状与缺口)
12. [前端对接要点（给 AI 的关键约束）](#12-前端对接要点给-ai-的关键约束)
13. [部署与运行](#13-部署与运行)
14. [测试](#14-测试)
15. [配置项](#15-配置项)

---

## 1. 项目概览

**校园生活平台**是一个面向高校师生的全功能服务聚合系统，覆盖以下业务域：

| 业务域 | 说明 |
| --- | --- |
| 🏷 二手交易 | 物品发布/搜索/下架，买家-卖家交易会话撮合（`items` + `trade_sessions`） |
| 💬 实时消息 | WebSocket 实时单聊/交易会话，多端在线、断线补偿、已读回执（`conversations`/`participants`/`messages`） |
| 📚 课程评价 | 课程库 + 星级评价（`courses` + `course_reviews`） |
| 🍚 食堂 | 食堂 → 摊位 → 菜品三级目录 + 菜品评价（`canteens`/`stalls`/`dishes` + `canteen_reviews`） |
| 💼 兼职 | 岗位发布/申请/录用管理（`jobs` + `job_applications`） |
| 📄 资源共享 | 学习资料上传/下载（`share_resources`） |
| 🤝 队友招募 | 竞赛/项目组队（`teams` + `team_members`） |
| 🚨 举报 | 用户/物品/消息/评论/资源举报工单 + 处理流水（`reports` + `report_logs`） |
| 👤 账户体系 | 邮箱/手机/自定义账号 + 密码统一登录，QQ/微信 OAuth，邮箱验证注册（`users`/`oauth_accounts`/`refresh_tokens`/`verification_codes`） |
| 🛡 管理后台 | 用户封禁/解封、举报处理、仪表盘、邮箱注册配置、物品管理（上下架/删除/发布审核开关）（`admin` 模块） |

**用户角色**：普通用户（可发布/交易/聊天/评价/申请）与管理员（登录 admin 后台，操作管理接口）。

**架构形态**：前后端分离的单体后端（模块化单体，不是微服务）。后端一个 FastAPI 进程承载全部 REST API + WebSocket，Celery worker 处理异步任务。

---

## 2. 技术栈总览

### 后端（Python 3.12）

| 组件 | 技术 | 版本/说明 |
| --- | --- | --- |
| Web 框架 | FastAPI | uvicorn ASGI，REST + WebSocket 同进程 |
| ORM | SQLAlchemy 2.0 | 异步 `async` 风格，`Mapped`/`mapped_column` |
| 数据校验 | Pydantic v2 | `BaseModel` + `model_config = {"from_attributes": True}` |
| 任务队列 | Celery | broker/result 均为 Redis |
| 缓存/验证码/限流/WS 广播 | Redis | 单实例多 db（见 §8） |
| 对象存储 | MinIO | 预签名直传；无 MinIO 时降级本地磁盘 |
| 全文搜索 | Meilisearch | 物品/用户搜索；不可用时降级 SQL LIKE |
| 数据库 | PostgreSQL(asyncpg) / 本地 SQLite(aiosqlite) | 由 `DB_URL` 决定 |
| 密码 | 自研哈希（`security.py`） | pbkdf2 风格 |
| JWT | 自研签发/校验 | access + refresh，Redis 黑名单吊销 |

### 前端（现状）

| 组件 | 技术 | 说明 |
| --- | --- | --- |
| 框架 | Vue 3 + Vite 5 | `<script setup>` 组合式 API + TypeScript |
| UI 库 | Element Plus | 管理后台风格 |
| 状态管理 | Pinia | `auth`（登录态）、`ws`（实时消息） |
| HTTP | axios | 统一实例封装 |
| 实时 | 原生 WebSocket | `/ws`，见 §8.5 |
| 路由 | vue-router 4 | 现有为管理后台路由 |

前端目前只有**管理后台雏形**（登录页 + 若干管理页），普通用户端完整页面尚未开发——这正是要交给 Google AI Studio 补全的部分。

### 中间件/基础设施

| 组件 | 用途 |
| --- | --- |
| Nginx | 生产部署：静态前端 + 反向代理 `/api`、`/ws` |
| Docker Compose | 一键编排：api / worker / redis / minio / meilisearch / postgres / nginx / prometheus / grafana |
| Prometheus + Grafana | `/metrics` 指标采集与监控面板（Prometheus 格式暴露） |

---

## 3. 系统架构与请求链路

```
浏览器(前端 SPA)
   │  /api/* (REST)                      /ws (WebSocket)
   ▼                                      ▼
┌─ Nginx (生产) / Vite dev proxy (开发: /api, /ws → :8000) ─┐
   ▼                                                        ▼
┌─────────────── FastAPI 单体 (uvicorn :8000) ────────────────┐
│  REST 路由 → 中间件(日志/限流/公开路径) → 模块 service → DB   │
│  WebSocket /ws → ConnectionManager → Redis Pub/Sub 广播      │
│  /metrics → Prometheus 指标                                  │
└───────┬──────────────────┬─────────────────┬───────────────┘
        ▼                  ▼                 ▼
  PostgreSQL/SQLite   Redis(6379)     MinIO(9000/9001) / 本地磁盘
                                      
Celery Worker（独立进程，-P solo，消费 email/notify/search/default 队列）
   └─ 邮件发送 / 站内通知 / Meilisearch 索引同步 / 交易摘要
```

**请求链路（一次发布物品）**：
1. 前端先 `POST /api/files/presign`（带 Bearer token）拿 `object_key` + 预签名上传 URL，浏览器直传 MinIO（本地降级则 `POST /api/files/upload` 由后端落盘）。
2. `POST /api/items` 携带 `images: [{object_key, sort_order}]` 创建物品。
3. 创建后 Celery 任务 `index_item` 同步进 Meilisearch，供 `GET /api/items/search` 检索。
4. 其他用户通过 `POST /api/items/{id}/trade` 发起交易 → 创建 `TradeSession` + 自动建会话 → 双方经 WebSocket 实时聊天。

---

## 4. 目录结构

```
campusLifePlatform-py/
├── backend/                      # FastAPI 后端
│   ├── app/
│   │   ├── main.py               # FastAPI 应用装配（路由挂载、中间件、lifespan）
│   │   ├── asgi.py               # ASGI 入口（uvicorn 目标）
│   │   ├── core/                 # 基础设施
│   │   │   ├── config.py         # Settings（yaml + env 覆盖）
│   │   │   ├── database.py       # AsyncEngine / SessionLocal / get_db
│   │   │   ├── middleware.py     # 网关中间件（日志/限流/PUBLIC_PATHS）
│   │   │   ├── response.py       # ApiResponse 统一包装
│   │   │   ├── exceptions.py     # BizError + 全局异常 handler
│   │   │   ├── security.py       # 密码哈希 / JWT 签发校验 / 吊销
│   │   │   ├── redis.py          # get_redis / redis_publish / redis_subscribe
│   │   │   ├── storage.py        # 对象存储（MinIO / 本地降级）
│   │   │   ├── search.py         # Meilisearch 客户端（可禁用降级）
│   │   │   ├── logging.py        # 结构化日志
│   │   │   ├── sync_db.py        # 同步 URL 转换（供 Celery/PS）
│   │   │   └── seed.py           # 默认管理员种子数据
│   │   ├── common/
│   │   │   ├── models.py         # Base / PKMixin / TimestampMixin / SoftDeleteMixin
│   │   │   └── enums.py          # 全部状态枚举（见 §6.2）
│   │   ├── modules/              # 业务模块（每个含 models/schemas/service/router）
│   │   │   ├── auth/  user/  admin/
│   │   │   ├── item/  message/  report/
│   │   │   ├── course/  canteen/  job/  share/  teammate/
│   │   │   ├── storage/          # 文件上传
│   │   │   └── launcher/         # /health /metrics
│   │   └── tasks/                # Celery 任务
│   │       ├── celery_app.py     # Celery 实例（含 Windows Lock 兼容补丁）
│   │       ├── email.py  notify.py  search_sync.py  summary.py
│   ├── scripts/                  # 工具脚本（fake redis / API 文档生成等）
│   └── tests/                    # pytest 测试（19 个测试文件，113 passed）
├── config/school.yaml            # 学校配置（含 auth.email_register 默认值）
├── deploy/                       # docker-compose / nginx / Dockerfile 等
└── docs/                         # API_Reference.md / openapi.json / 本报告
```

---

## 5. 当前运行服务与端口

| 服务 | 地址 | 状态 |
| --- | --- | --- |
| 前端（frontend） | http://localhost:5173 | ✅ 运行中 |
| 后端 API | http://localhost:8000 | ✅ 运行中 |
| API 文档（Swagger） | http://localhost:8000/docs | ✅ |
| 健康检查 | http://localhost:8000/health | `{"status":"ok","database":"up",...}` |
| Redis 兼容服务 | 127.0.0.1:6379 | ✅ fakeredis TCP Server（本机无真实 Redis） |
| Celery worker | — | ✅ `-P solo`，4 队列 |
| MinIO | 未启用 | 本地降级（文件存磁盘，`/api/files/raw` 下载） |
| Meilisearch | 未启用 | 降级 SQL LIKE 搜索 |
| 数据库 | SQLite（本地 dev） | 生产为 PostgreSQL |

**本地开发连接串**：`backend/.env` → `DB_URL=sqlite+aiosqlite:///./campus.db`；生产 compose 用 PostgreSQL。

---

## 6. 数据库设计

数据库 14 张表。主键均为 UUID（`PKMixin`），`TimestampMixin` 提供 `created_at`/`updated_at`，`SoftDeleteMixin` 提供 `is_deleted`（软删）。用户 ID 在业务表以 `String(36)` 存 UUID 文本。

### 6.1 表清单与字段

**账户体系（auth 模块）**

| 表 | 字段（除 id/created_at/updated_at） | 说明 |
| --- | --- | --- |
| `users` | `username`(unique), `email`(unique,可空), `phone`(unique,可空), `password_hash`, `nickname`, `avatar`(可空), `status`(int), `is_deleted` | 平台用户，一表承载全部登录方式 |
| `oauth_accounts` | `user_id`→users, `provider`(wechat/qq), `provider_openid`, `linked_at` | 第三方绑定 |
| `refresh_tokens` | `user_id`, `jti`(unique), `expires_at`, `revoked` | 刷新令牌吊销追踪 |
| `verification_codes` | `target`(邮箱/手机), `code`, `purpose`(login/register/email/bind_*), `expires_at`, `attempts` | 验证码（Redis + 表双写） |

**二手交易（item 模块）**

| 表 | 字段 | 说明 |
| --- | --- | --- |
| `items` | `owner_id`→users, `title`, `description`, `price`(**单位：分**), `category`(默认 other), `status`(ItemStatus), `is_deleted` | 二手物品 |
| `item_images` | `item_id`→items, `object_key`, `sort_order` | 图片（对象存储 key） |
| `trade_sessions` | `item_id`→items, `buyer_id`→users, `seller_id`→users, `status`(TradeStatus), `conversation_id`(可空,关联消息会话) | 交易撮合 |

**消息（message 模块）**

| 表 | 字段 | 说明 |
| --- | --- | --- |
| `conversations` | `conv_type`(direct/trade/group), `related_id`(可空) | 会话；`(conv_type,related_id)` 唯一 |
| `participants` | `conversation_id`, `user_id`, `last_read_at` | 参与者 + 已读游标；`(conv,user)` 唯一 |
| `messages` | `conversation_id`, `sender_id`, `type`(0文本/1图片/2文件), `content`(Text), `is_read` | 消息 |

**课程（course 模块）**

| 表 | 字段 |
| --- | --- |
| `courses` | `code`(unique), `name`, `teacher`, `credits`, `semester` |
| `course_reviews` | `course_id`, `user_id`, `rating`(1-5), `content` |

**食堂（canteen 模块）**

| 表 | 字段 |
| --- | --- |
| `canteens` | `name`, `location` |
| `stalls` | `canteen_id`, `name` |
| `dishes` | `stall_id`, `name`, `price`(**分**) |
| `canteen_reviews` | `dish_id`, `user_id`, `rating`, `content` |

**兼职（job 模块）**

| 表 | 字段 |
| --- | --- |
| `jobs` | `poster_id`, `title`, `description`, `company`, `salary`(**分**), `category`, `status`(JobStatus) |
| `job_applications` | `job_id`, `applicant_id`, `status`(ApplicationStatus), `note` |

**资源共享（share 模块）**

| 表 | 字段 |
| --- | --- |
| `share_resources` | `owner_id`, `title`, `description`, `file_key`(对象存储 key), `category`, `downloads` |

**队友招募（teammate 模块）**

| 表 | 字段 |
| --- | --- |
| `teams` | `creator_id`, `title`, `description`, `required_roles`, `status`(TeamStatus) |
| `team_members` | `team_id`, `user_id`, `role`, `status`(MemberStatus) |

**举报（report 模块）**

| 表 | 字段 |
| --- | --- |
| `reports` | `reporter_id`, `target_type`(user/item/message/comment/share), `target_id`, `reason`, `status`(ReportStatus), `handled_by` |
| `report_logs` | `report_id`, `operator_id`, `action`, `note` |

**配置（admin 模块）**

| 表 | 字段 |
| --- | --- |
| `app_config` | `key`(unique,如 `auth.email_register`), `value`(JSON) | 后台可配置项，优先于 yaml |

### 6.2 枚举/状态机（前端必须按此映射中文文案）

| 枚举 | 值 | 含义 |
| --- | --- | --- |
| `UserStatus` | 0/1/2 | 正常/封禁/待审核 |
| `ItemStatus` | 0/1/2/3/4 | 上架/下架/已售/保留/待审核（4 仅在后台开启发布审核开关后出现） |
| `TradeStatus` | 0/1/2/3 | 待处理/进行中/已完成/已取消 |
| `MessageType` | 0/1/2 | 文本/图片/文件 |
| `JobStatus` | 0/1 | 招聘中/已关闭 |
| `ApplicationStatus` | 0/1/2 | 待处理/已录用/已拒绝 |
| `TeamStatus` | 0/1/2 | 招募中/已满员/已解散 |
| `MemberStatus` | 0/1/2 | 待确认/已加入/已退出 |
| `ReportStatus` | 0/1/2/3 | 待处理/处理中/已解决/已驳回 |
| `ReportTargetType` | user/item/message/comment/share | 举报目标类型（字符串） |
| `ConversationType` | direct/trade/group | 会话类型（字符串） |

---

## 7. 认证与授权

### 7.1 登录方式（全部支持，绑定到同一账户）

1. **账号密码登录**：`POST /api/auth/login`，body `{account, password}`。`account` 可以是 **自定义用户名 / 邮箱 / 手机号** 任一（后端统一查询）。兼容旧字段 `username`。
2. **手机验证码登录**：`POST /api/auth/send-code`（purpose=`login`）→ `POST /api/auth/phone-login` `{target, code}`。
3. **QQ/微信 OAuth**：`GET /api/auth/wechat/state`（或 qq）拿 state → 跳第三方 → `GET /api/auth/wechat/callback?code=&state=` 换 token。
4. **邮箱验证码注册**：`POST /api/auth/send-code`（purpose=`register`，target=邮箱）→ `POST /api/auth/email-register` `{email, password, code, nickname?}`。邮箱域名须在白名单（后台可配）。

### 7.2 补充绑定 / 解绑

- `GET /api/auth/bindings`：查看当前账户已绑定的 username/email/phone/oauth。
- `POST /api/auth/bind/email` `{email, code}`（验证码 purpose=`bind_email`）。
- `POST /api/auth/bind/phone` `{phone, code}`（purpose=`bind_phone`）。
- `POST /api/auth/bind/oauth` `{provider, code, redirect_uri?, state?}`。
- `DELETE /api/auth/unbind/email|phone|oauth`。
- **冲突策略**：若绑定目标已关联其他账户 → 拒绝绑定，返回 `code=40900`，message 明确提示"该邮箱/手机号已绑定其他账号"。解绑后可复用。不会自动合并账户。

### 7.3 JWT 流程

```
登录成功 → {access_token, refresh_token, token_type:"bearer", expires_in}
调用业务接口 → Header: Authorization: Bearer <access_token>
access 过期(40100) → POST /api/auth/refresh {refresh_token} → 新 token 对
登出 → POST /api/auth/logout（吊销 refresh；access 进入 Redis 黑名单至过期）
```

- `access_token`：短期（约 30 分钟），payload 含 `type=access`、`sub=user_id`。
- `refresh_token`：长期（约 7 天），`type=refresh`、`jti` 入库可吊销。
- 刷新时后端校验 refresh 的 jti 未吊销 → 旋转新对。
- **前端必须**：401 时静默刷新一次（刷新也失败则清登录态跳登录页）；token 存 localStorage（可刷新保留）。

### 7.4 验证码

- 发送：`POST /api/auth/send-code` `{target, purpose}`，purpose 取值 `login` / `register` / `bind_email` / `bind_phone`。
- 校验：后端按 `target+purpose` 匹配，有发送间隔与次数限制（Redis 计数）。
- 前端 UI：发送按钮 60s 倒计时是常见做法（后端限流兜底）。

### 7.5 权限模型

- 接口分三层：**公开**（登录/注册/验证码/OAuth state/callback/health）、**登录**（业务接口，`Authorization: Bearer`）、**管理员**（`/api/admin/*`，校验 `users.username` 为内置管理员）。
- 管理员账户由 `app/modules/admin/service.py` 的 `ensure_seed` 按 `config/school.yaml` 的 `admin.bootstrap` 段（用户名/密码）种子写入，**不再硬编码**，生产由部署配置下发强密码；`/api/admin/*` 还需网关令牌（见 §管理员域），未携带有效令牌一律返回 404。
- `GET /api/admin/me` 返回管理员信息；普通用户调用 admin 接口返回 40300。

---

## 8. 中间件与基础设施

### 8.1 网关中间件（`core/middleware.py`）

- 全局 HTTP 日志（方法/路径/状态/耗时）。
- 限流：`rate_limit_per_minute`（按 IP + 路径计数，Redis，默认如 120/分钟；超限 429）。
- **PUBLIC_PATHS 白名单**：未登录请求仅放行公开路径（含 `/api/auth/*` 登录注册等、`/api/files/raw`、`/health`、`/docs`、`/metrics` 等），其余一律 40100。
- 注意：`/api/auth/*` 整体公开，但绑定/解绑类接口内部仍校验登录（路由级 Depends）。

### 8.2 Redis（127.0.0.1:6379，多 db）

| db | 用途 |
| --- | --- |
| db0 | 默认缓存/验证码等 |
| db1 | Celery broker（`redis://.../1`） |
| db2 | Celery result backend（`/2`） |

无 Redis 时后端自动内存兜底（验证码、限流降级）；本机当前用 fakeredis TCP Server 模拟（`backend/scripts/fake_redis_server.py`）。

### 8.3 对象存储（`core/storage.py`）

- **MinIO 模式**（生产）：`POST /api/files/presign` 返回 `{object_key, upload_url, direct:true}`，浏览器直传 `upload_url`；展示用 `presigned_download_url`（图片/文件均需登录后换取签名 URL）。
- **本地降级模式**（当前 dev）：`direct:false` → 客户端 `POST /api/files/upload`（multipart：`key` + `file`），下载走 `GET /api/files/raw?key=`。
- 图片对象 key 形如 `items/{uuid}.jpg`；`ItemImage.object_key` 存 key，不存 URL（URL 每次实时签名）。

### 8.4 Celery 任务（`app/tasks/`，7 个注册任务）

| 任务 | 队列 | 用途 |
| --- | --- | --- |
| `tasks.send_verification_email` | email | 发送邮箱验证码邮件 |
| `tasks.send_email` | email | 通用邮件 |
| `tasks.send_notify` | notify | 站内/WS 通知（参数 user_id, title, content, channel） |
| `tasks.index_item` | search | 物品进 Meilisearch |
| `tasks.index_user` | search | 用户进 Meilisearch |
| `tasks.remove_item_from_index` | search | 物品出索引 |
| `tasks.generate_daily_summary` | default | 每日交易摘要（周期任务） |

- worker 启动：`celery -A app.tasks.celery_app.celery_app worker --loglevel=info -P solo -Q email,notify,search,default`（**Windows 必须 `-P solo`**）。
- 邮件/通知未配置 SMTP 时任务会失败并进入重试/死信（本地可忽略，不影响主流程）。

### 8.5 WebSocket 实时消息协议（`/ws`）

- **连接**：`ws://host/ws?token=<access_token>&since=<ISO8601>`（token 必填，`since` 用于断线补偿可选）。
- **鉴权失败**：服务器发 `{"event":"error","data":{"code":40100,...}}` 后关闭连接。
- **客户端 → 服务器**：
  - `{"event":"ping"}` → 服务器回 `{"event":"pong"}`
  - `{"event":"message:send","data":{"conversation_id","content","type"(0/1/2)}}` → 持久化并广播
  - `{"event":"message:read","data":{"conversation_id"}}` → 回执 `message:read_ack`
- **服务器 → 客户端**：
  - `{"event":"message:new","data":{"id","conversation_id","sender_id","type","content","created_at"}}`
  - `{"event":"message:read_ack","data":{"conversation_id","user_id"}}`
  - `{"event":"error","data":{"code","message"}}`
- **断线补偿**：重连带 `since` → 服务器补推该会话之后的消息（最多 100 条）。
- 多端在线支持（同一用户多连接）；Redis Pub/Sub 跨实例广播。

### 8.6 搜索（`core/search.py`）

- Meilisearch 索引：`items`（按标题/描述）、`users`。
- 接口 `GET /api/items/search?q=`、`GET /api/users/search?q=` 优先走 Meilisearch，未启用时降级 SQL LIKE。
- 搜索同步由 Celery 任务维护（创建/删除物品时）。

---

## 9. API 规范与约定

### 9.1 统一响应格式（**最重要约定，前端必须遵守**）

所有 REST 接口返回：

```json
{ "code": 0, "message": "ok", "data": { ... } }
```

- 成功 `code=0`。
- **业务错误也是 HTTP 200**，靠 `code` 区分，`message` 为可展示中文。前端拦截器判断 `code !== 0` 即业务失败。
- 仅网络层/异常层（如 500、429 限流、422 FastAPI 校验）会出现非 200 状态码。

### 9.2 错误码

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 40000 | 参数/业务错误（通用） |
| 40100 | 未认证（token 缺失/过期/吊销） |
| 40300 | 禁止访问（权限不足） |
| 40400 | 资源不存在 |
| 40900 | 冲突（绑定冲突等） |
| 42200 | 校验失败（Pydantic） |
| 42900 | 限流 |

### 9.3 分页约定

列表接口统一 query 参数：`page`（默认 1，≥1）、`page_size`（默认 20，≤100）。
`data` 形如 `{"total": n, "items": [...]}`（部分接口为数组，以 API_Reference 为准）。

### 9.4 鉴权头

```
Authorization: Bearer <access_token>
```

### 9.5 金额单位

**`price`/`salary` 单位一律为"分"（int）**，前端展示需 ÷100 并保留两位；提交也传分。

### 9.6 时间格式

ISO 8601 字符串（`datetime.isoformat()`），如 `2026-08-25T10:30:00+08:00`。

---

## 10. API 模块清单

共 **88 个接口 / 14 个 tag**（全部接口字段级文档见 `docs/API_Reference.md`）。

### 10.1 auth（19 个，公开 + 登录）

| 接口 | 说明 |
| --- | --- |
| `POST /api/auth/register` | 用户名+密码注册（可带 email/phone） |
| `POST /api/auth/login` | 统一登录 `{account, password}` |
| `POST /api/auth/phone-login` | 手机验证码登录 `{target, code}` |
| `POST /api/auth/refresh` | 刷新 token |
| `POST /api/auth/logout` | 登出（吊销） |
| `POST /api/auth/send-code` | 发送验证码 `{target, purpose}` |
| `POST /api/auth/verify-email` | 邮箱验证（JWT token） |
| `GET /api/auth/wechat/state` `GET /api/auth/qq/state` | OAuth state |
| `GET /api/auth/wechat/callback` `GET /api/auth/qq/callback` | OAuth 回调换 token |
| `POST /api/auth/email-register` | 邮箱验证码注册 `{email, password, code, nickname?}` |
| `GET /api/auth/bindings` | 我的绑定列表 |
| `POST /api/auth/bind/email` `POST /api/auth/bind/phone` `POST /api/auth/bind/oauth` | 补充绑定 |
| `DELETE /api/auth/unbind/email` `DELETE /api/auth/unbind/phone` `DELETE /api/auth/unbind/oauth` | 解绑 |

### 10.2 user（4）

`GET /api/users/me`（我的资料）· `PATCH /api/users/me`（改资料：nickname/avatar）· `GET /api/users`（分页列表）· `GET /api/users/search?q=`（搜用户）

### 10.3 admin（9，需管理员）

`POST /api/admin/login`（管理员登录）· `GET /api/admin/me` · `GET /api/admin/dashboard`（仪表盘统计）· `GET /api/admin/users` · `POST /api/admin/users/{id}/ban|unban` · `GET/PUT /api/admin/auth/email-config`（邮箱注册配置）· `GET /api/admin/items`（全量物品列表，含状态筛选）· `PATCH /api/admin/items/{id}`（上下架）· `DELETE /api/admin/items/{id}`（删除）· `GET/PUT /api/admin/items/review-config`（发布审核开关，DB 覆盖 school.yaml `items.review`）· `POST /api/admin/items/{id}/approve`（审核通过：PENDING→上架）· `POST /api/admin/items/{id}/reject`（审核拒绝：PENDING→下架）· `GET /api/admin/reports`

### 10.4 item（7，登录）

`POST /api/items`（发布，body 含 `images:[{object_key,sort_order}]`）· `GET /api/items`（分页 + keyword/category/status 过滤）· `GET /api/items/search?q=` · `GET /api/items/{id}` · `PATCH /api/items/{id}`（改价/上下架）· `DELETE /api/items/{id}` · `POST /api/items/{id}/trade`（发起交易，建 TradeSession+会话）

### 10.5 message（4，登录）

`GET /api/messages/conversations`（会话列表）· `GET /api/messages/conversations/{id}`（历史消息）· `POST /api/messages/conversations/{id}/read`（标记已读）· `GET /api/messages/unread`（未读数）

### 10.6 course（4）

`GET /api/courses` · `POST /api/courses` · `GET /api/courses/{id}` · `POST /api/courses/{id}/reviews`

### 10.7 canteen（6）

`GET /api/canteens`（含 stalls/dishes 级联）· `POST /api/canteens` · `POST /api/canteens/stalls` · `POST /api/canteens/dishes` · `GET /api/canteens/dishes/{id}` · `POST /api/canteens/dishes/{id}/reviews`

### 10.8 job（4）

`GET /api/jobs` · `POST /api/jobs` · `POST /api/jobs/{id}/apply` · `GET /api/jobs/{id}/applications`（发布者查申请）

### 10.9 share（3）

`GET /api/shares` · `POST /api/shares`（上传文件 key）· `GET /api/shares/{id}/download`（预签名下载）

### 10.10 teammate（4）

`GET /api/teams` · `POST /api/teams` · `GET /api/teams/{id}` · `POST /api/teams/{id}/join`

### 10.11 report（3，登录）

`POST /api/reports`（`{target_type, target_id, reason}`）· `GET /api/reports`（我的举报）· `POST /api/reports/{id}/handle`（管理员处理）

### 10.12 files（3，登录）

`POST /api/files/presign`（multipart form：prefix, filename）· `POST /api/files/upload`（multipart：key, file）· `GET /api/files/raw?key=`（本地降级下载）

### 10.13 launcher（2）

`GET /health` · `GET /metrics`（Prometheus）

### 10.14 默认（1）

`GET /`（root 信息）

---

## 11. 前端现状与缺口

### 11.1 现状（已有）

- **技术栈**：Vue3 `<script setup>` + TS + Vite5 + Element Plus + Pinia + vue-router4 + axios + 原生 WebSocket。
- **文件结构**：
  - `src/main.ts`（应用入口，挂 Pinia/router/ElementPlus）
  - `src/router/index.ts`（管理后台路由）
  - `src/Layout.vue`（后台布局：侧边栏+顶栏）
  - `src/api/`：`request.ts`（axios 实例，拦截器）、`auth.ts`/`item.ts`/`report.ts` 等 API 封装
  - `src/stores/`：`auth.ts`（token+用户态）、`ws.ts`（WebSocket 连接管理）
  - `src/views/`：`Login.vue` + 若干管理页
- **request.ts 拦截器**：请求带 `Authorization: Bearer`，响应统一解 `{code,message,data}`，`code!==0` 抛业务错误。
- **ws.ts**：连接 `/ws?token=`，ping/pong 保活，message:new 事件分发。

### 11.2 缺口（待开发，即 AI 的任务）

| 页面/能力 | 状态 |
| --- | --- |
| 普通用户注册（用户名/邮箱验证码注册） | ❌ 未开发 |
| 手机验证码登录 / 统一登录入口 | ❌ |
| OAuth 微信/QQ 跳转登录 | ❌ |
| 我的绑定管理页（bindings/绑定/解绑） | ❌ |
| 二手市场：列表/搜索/详情/发布/交易 | ❌ |
| 消息：会话列表/聊天窗口/未读数/已读回执 | ❌ |
| 课程评价 / 食堂目录与评价 / 兼职 / 资源共享 / 队友招募 | ❌ |
| 举报入口与我的举报 | ❌ |
| 个人中心（资料修改/头像上传） | ❌ |

即：**现有前端仅覆盖管理后台（admin），普通用户端全部页面需新建**。建议直接替换/重构前端为面向普通用户 + 管理双入口的完整应用。

---

## 12. 前端对接要点（给 AI 的关键约束）

1. **统一响应拦截**：所有接口 `{code,message,data}`；`code===0` 成功，否则读 `message` 提示。HTTP 状态码不可信（业务错误是 200）。网络层错误（无响应）单独处理。
2. **401 处理**：拦截 `code===40100` → 尝试 `POST /api/auth/refresh`（带 refresh_token）→ 成功后重放原请求；刷新失败则清 token 跳登录页。refresh_token 也要过期，需兜底登出。
3. **Token 存储**：`access_token` + `refresh_token` 存 localStorage（键名与现有 `stores/auth.ts` 保持一致或重构）。axios 请求拦截器统一加 `Authorization: Bearer <access_token>`。
4. **金额**：所有 price/salary 是"分"，展示 ÷100，格式 `¥12.50`。
5. **枚举映射**：§6.2 的状态值在 UI 全部转中文标签 + 颜色（如 item.status 0=上架绿色，1=下架灰，2=已售橙，3=保留蓝）。
6. **图片/文件上传**：
   - 先 `POST /api/files/presign`（form：prefix=items/shares/avatars, filename）拿 `{object_key, upload_url, direct}`。
   - `direct:true` → PUT 文件到 `upload_url`（MinIO 直传）；`direct:false` → `POST /api/files/upload`（FormData：key+file）。
   - 提交业务（发布物品）时传 `object_key` 而非 URL。展示图片需重新 `presign` 拿下载签名 URL。
7. **WebSocket**：
   - 登录后连 `/ws?token=<access_token>`；收 `message:new` 更新聊天列表。
   - 心跳：每 30s 发 `{"event":"ping"}`，收 `pong` 保活；断线指数退避重连，重连带 `since=<最后消息时间>` 做补偿。
   - 发消息：`{"event":"message:send","data":{...}}`；自己发的消息不依赖广播回显，可乐观插入。
   - 会话详情用 REST `GET /api/messages/conversations/{id}` 拉历史。
8. **分页**：列表页用 `page`/`page_size` 参数，响应 `data.total`/`data.items`。
9. **管理员域**：`/api/admin/*` 受网关保护——需先在 `/api/admin/discover` 用部署下发的网关密钥换取短时令牌，并携带 `X-Admin-Gateway` 头；未携带有效令牌一律返回 404。管理员凭证由 `config/school.yaml` 的 `admin.bootstrap` 配置下发（不再固定 admin/admin123）。普通前端不暴露 admin 入口与敏感路由。
10. **可用性设计**：Meilisearch/MinIO 未部署时后端会自动降级（SQL 搜索/后端落盘），前端无需感知，但**搜索接口**在没有 Meilisearch 时返回的是 SQL 结果结构（`items` 数组），需与 Meilisearch 返回兼容处理（都以 items 数组为准）。

---

## 13. 部署与运行

### 13.1 本地开发

```bash
# 后端（backend/）
pip install -r requirements.txt   # 或 pip install -e ".[dev]"
python scripts/fake_redis_server.py          # 无真实 Redis 时
uvicorn app.asgi:app --host 127.0.0.1 --port 8000
celery -A app.tasks.celery_app.celery_app worker --loglevel=info -P solo -Q email,notify,search,default

# 前端（frontend/，React 19 + Vite + Express 代理层）
npm install
npm run dev   # http://localhost:5173，/api /ws 经 Express 代理到 8000
```

### 13.2 生产（deploy/docker-compose.yml）

服务：`api`（uvicorn + 后端）、`worker`（celery，挂 `config:/app/config:ro`）、`redis`、`minio`、`meilisearch`、`postgres`、`nginx`、`prometheus`、`grafana`。环境变量 `DB_URL=postgresql+asyncpg://...`、`REDIS_URL`、`MINIO_*`、`MEILI_*` 在 compose 中配置。首次启动自动建表 + seed 管理员。

### 13.3 关键环境变量（backend/.env 或 compose）

| 变量 | 说明 |
| --- | --- |
| `DB_URL` | `sqlite+aiosqlite:///./campus.db`（dev）或 `postgresql+asyncpg://user:pass@host:5432/db` |
| `REDIS_URL` | `redis://127.0.0.1:6379/0`（broker 用 `/1`，backend 用 `/2`，代码内约定） |
| `MINIO_ENDPOINT`/`MINIO_ACCESS_KEY`/`MINIO_SECRET_KEY`/`MINIO_BUCKET` | 对象存储；缺省自动本地降级 |
| `MEILI_HOST`/`MEILI_API_KEY` | 搜索；缺省 SQL 降级 |
| `SMTP_*` | 邮件发送；缺省发信失败走 Celery 重试 |
| `SECRET_KEY` | JWT 签名密钥 |
| `SCHOOL_CONFIG_PATH` | `config/school.yaml` 路径 |
| `WECHAT_APP_ID`/`WECHAT_APP_SECRET`/`QQ_APP_ID`/`QQ_APP_SECRET` | OAuth 应用凭证 |

---

## 14. 测试

- 后端 pytest：**113 个测试全部通过**（认证/邮箱注册/绑定、滑块验证、OAuth、WS 推送、Celery 摘要、限流、生命周期与资源释放等）。
- 测试文件：`backend/tests/`（`conftest.py` 提供 `sync_session_factory`、`ws_client`、放宽限流 fixture）。
- 运行：`cd backend && python -m pytest -q`。

---

## 15. 配置项

### 15.1 config/school.yaml（学校级默认配置）

```yaml
school:
  name: 示例大学
auth:
  email_register:
    enabled: true
    domains: [example.edu.cn, example.com]   # 邮箱注册域名白名单
    pattern: ""                              # 可选正则（fullmatch），非空则按正则校验
```

后台 `GET/PUT /api/admin/auth/email-config` 可覆盖（写入 `app_config` 表，key=`auth.email_register`，DB 优先于 yaml）。

### 15.2 其他

- 限流：`rate_limit_per_minute`（中间件配置，默认 120/分钟/IP/路径）。
- JWT 有效期：access ≈ 30min，refresh ≈ 7d（`core/config.py` 可调）。
- 验证码：发送间隔、有效期、尝试次数（Redis + `verification_codes` 表）。

---

*报告生成时间：2026-08-25 | 数据来源：backend 源码 + 运行中服务 /openapi.json | 配套：docs/API_Reference.md、docs/openapi.json*
